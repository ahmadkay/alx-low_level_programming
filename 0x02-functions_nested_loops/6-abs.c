#include "main.h"

/**
 * _abs - Check the code
 * @n: An input interger
 * Description: function uses _putchar function to print
 * Return: returns 0.
 */



int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (n * -1);
}
